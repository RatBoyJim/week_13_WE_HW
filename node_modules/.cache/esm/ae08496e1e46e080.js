_1e4‍.x([["default",()=>_1e4‍.o]]);class Library {
  constructor(books = []) {
    this.books = books;
  }
  bookCount(){
    return this.books.length;
  }

  addBook(newBook){
    this.books.push(newBook);
  }

  addBooks(newBooks){
    newBooks.forEach(book => this.books.push(book));
  }

  printInventory(){
    const {author, title} = book;
    this.books.forEach((book) => {
      console.log(`${title} by ${author}`);
    });
  }
};

_1e4‍.d(Library);
